# Plan 05 — completion report

- Plan file: `plans/05-vision-freshness.md`
- Date / commit or working-tree identification: 2026-09-21; working tree based on `f60c735`
- Code status: complete
- Automated verification: passed
- Human acceptance: pending R01/R02/R03/R05

## Changes and contracts

- Added `VisionFaceDetector`, serial `FrameAnalyzer`, and capacity-one `ObservationMailbox`. Revision 3 is pinned; pose requires complete optional yaw/pitch/roll and converts radians to degrees once.
- Camera start now receives `FrameAnalysisContext`. Every frame snapshots session ID, geometry revision, viewport, delivered rotation, clean aperture, and one-mirror policy. Pixel buffers and Vision objects remain synchronous and never enter store state.
- `FrameObservation` distinguishes monotonic capture arrival from successful result time. Analysis and mailbox consumption independently withdraw payload only when age is greater than 300 ms. Lighting stays nil.
- Added injected monotonic scheduler and `AnalysisWatchdog`: 250 ms cadence, strict greater-than-5,000 stall, strict greater-than-300 freshness, expected-sample protection, and cancellation on invalidation.
- Detector failures use the reliable camera event path; observations use the lossy newest-wins mailbox. Stop invalidates the analyzer/mailbox before capture teardown.
- Updated `Camera/AGENTS.md`, `CaptureUI/AGENTS.md`, and `facetrackingTests/AGENTS.md`. Research evidence is in `docs/implementation/evidence/05-vision-research.md`; R01/R02/R03/R05 remain OPEN.

## Verification evidence

| Test or requirement ID | Exact command or manual procedure | Environment / destination | Result | Evidence location |
| --- | --- | --- | --- | --- |
| Xcode MCP availability | `XcodeListWorkspaces` | Xcode 27.0; `facetracking.xcodeproj` | PASS — server enabled and workspace attached as session-local `windowtab-Sw2d2zEeFt` | This handoff (identifier intentionally not configuration) |
| Compile integration | Xcode MCP `BuildProject(buildForTesting: true)` | iPhone 17 simulator, iOS 27.0 | PASS | Xcode artifact log `BuildProject-Log-20260921-220911.txt` (temporary) |
| Detector/mailbox/S02/S04–S06/G02/full suite | Xcode MCP `RunAllTests` | iPhone 17 simulator, iOS 27.0 | PASS — 46 passed, 0 failed/skipped/not-run | `Test-facetracking-2026.09.21_22-14-54-+0530.xcresult` (temporary) |
| Unsigned device build first attempt | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project facetracking.xcodeproj -scheme facetracking -destination 'generic/platform=iOS' -derivedDataPath /tmp/facetracking-plan05-device CODE_SIGNING_ALLOWED=NO build` | sandboxed CLI, iOS 27 SDK, arm64 / minimum iOS 17 | FAILED — sandbox blocked Swift macro plugin (`sandbox_apply: Operation not permitted`) | Terminal output (temporary) |
| Unsigned device build rerun | same exact command, approved outside sandbox | iOS 27 SDK, arm64 / minimum iOS 17 | PASS — `** BUILD SUCCEEDED **` | `/tmp/facetracking-plan05-device` (temporary) |
| R01/R02 physical pose/crop/mirror | Procedure in `evidence/05-vision-research.md` | Minimum supported and newer physical iPhone | NOT RUN | Required counts/device metadata; no imagery |
| R03/R05 people/load/latency | Procedure in `evidence/05-vision-research.md` | Physical iPhone under scripted multi-person/load scenarios | NOT RUN | Required anonymous switch log and aggregate timing |

The Xcode run destination was restored to `Vishesh’s iPhone` after simulator tests. `Localizable.xcstrings` was unchanged.

## Human verification remaining

- R01/R02 (OPEN): use the recorded pose/geometry procedure on minimum and newer devices. Expected: complete-pose-only acquisition, correct signs, aligned mirror/edges, and documented nil-angle frequency. Actual: NOT RUN.
- R03/R05 (OPEN): run anonymous enter/cross/leave and load scenarios. Expected: documented first-usable switches, responsive bounded delivery, strict freshness, and p50/p95 timing. Actual: NOT RUN.
- Evidence must name device, OS, build/commit, request revision 3, full-range bi-planar YUV format, profile `ios-provisional-v1`, environment, and result. Do not save participant imagery.

## Decisions and research

- R01: OPEN. SDK/compile matrix and synthetic behavior recorded; physical pose reliability and signs are pending.
- R02: OPEN. G01/G02 remain passed; live rectangle/clean-aperture alignment is pending.
- R03: OPEN. First usable result order is implemented without identity assumptions; physical switching is pending.
- R05: OPEN. Monotonic capture/result times are distinct from sample presentation time; no timebase conversion is claimed and physical latency is pending.
- No R01–R07 gate was closed and no provisional threshold changed.

## Next chat

- Plan 06 can sample the same `CVPixelBuffer` inside `FrameAnalyzer.analyze`, after `FrameTransformSnapshot` creation and before synchronous return. The configured output requests `kCVPixelFormatType_420YpCbCr8BiPlanarFullRange`.
- Reuse that exact snapshot for luma cell-center mapping. Lock/read planes only within the callback; never retain a buffer, base address, Vision object, or borrowed pointer.
- Preserve the capacity-one owned observation and reliable failure channel. Lighting is still unavailable here; plan 06 may measure it but plan 07 owns heuristics/guidance.
- R01/R02/R03/R05 may remain open while independent plan 06 synthetic work proceeds, but dependent live acceptance cannot pass without their evidence.
