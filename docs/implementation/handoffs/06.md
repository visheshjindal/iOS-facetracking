# Plan 06 — completion report

- Plan file: `plans/06-lighting-measurement.md`
- Date / commit or working-tree identification: 2026-09-21; uncommitted working tree including completed plan 05, based on `f60c735`
- Code status: complete
- Automated verification: passed
- Human acceptance: pending R02/R04

## Changes and contracts

- Added `Camera/SparseLumaSampler.swift`. It supports only 8-bit bi-planar full/video-range YCbCr, locks plane zero read-only, honors actual dimensions/stride, samples a maximum 64×64 grid with continuous mapped cell centers, and owns reusable sample/workspace storage on the serial analyzer.
- Camera format negotiation prefers supported `420f`, falls back to supported `420v`, and leaves lighting unavailable for other delivered formats. Analyzer diagnostics retain only FourCC/range/plane layout/grid count plus existing aggregate timing—no pixels.
- Added pure `Tracking/LightingMeasurement.swift`: raw-face inner ellipse, mirrored viewport regions, five reusable 256-bin histograms, rank median, exact 10%-tail trimmed means, inclusive shadow/highlight fractions, and immutable metric validation.
- Metrics require complete finite pose, measurement pose eligibility, total count at least 96, all four regional counts at least 32, exact partitions, finite 0–255 means, and valid fractions. Missing/unsupported/stale inputs return nil and never reuse old metrics.
- Integrated sampling into the existing synchronous same-frame analyzer before buffer return. Stale analysis and stale mailbox consumption withdraw both face and lighting. Classification, persistence, warnings, advice, UI, and threshold tuning were not implemented.
- Updated `Camera/AGENTS.md`, `Tracking/AGENTS.md`, and `facetrackingTests/AGENTS.md`; added durable R02/R04 procedure/evidence and kept all research gates open.

## Verification evidence

| Test or requirement ID | Exact command or manual procedure | Environment / destination | Result | Evidence location |
| --- | --- | --- | --- | --- |
| Xcode MCP | `XcodeListWorkspaces` | Xcode 27.0; existing `facetracking.xcodeproj` | PASS — enabled and attached | Session-local MCP result |
| Initial compile attempt | Xcode MCP `BuildProject(buildForTesting: true)` | Active physical destination `Vishesh’s iPhone` | FAILED — test targets required a development team | `BuildProject-Log-20260921-222359.txt` (temporary) |
| Compile after selecting simulator | `XcodeSwitchRunDestination("iPhone 17 (27.0)")`; Xcode MCP `BuildProject(buildForTesting: true)` | iPhone 17 simulator, iOS 27.0 | PASS | `BuildProject-Log-20260921-222856.txt` (temporary) |
| Focused L01–L03/G02 | Xcode MCP `RunSomeTests`, 11 initial lighting/sampler tests | iPhone 17 simulator, iOS 27.0 | PASS — 11/11 | `Test-facetracking-2026.09.21_22-27-41-+0530.xcresult` (temporary) |
| Same-frame/stale integration and no-reuse | Xcode MCP `RunSomeTests`, 2 tests | iPhone 17 simulator, iOS 27.0 | PASS — 2/2 | `Test-facetracking-2026.09.21_22-29-04-+0530.xcresult` (temporary) |
| Full automated suite | Xcode MCP `RunAllTests` | iPhone 17 simulator, iOS 27.0 | PASS — 59 passed, 0 failed/skipped/not-run | `Test-facetracking-2026.09.21_22-33-23-+0530.xcresult` (temporary) |
| Unsigned generic-device build | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project facetracking.xcodeproj -scheme facetracking -destination 'generic/platform=iOS' -derivedDataPath /tmp/facetracking-plan06-device CODE_SIGNING_ALLOWED=NO build` | Xcode 27 / iOS 27 SDK, arm64, deployment target iOS 17 | PASS — `** BUILD SUCCEEDED **` | `/tmp/facetracking-plan06-device` (temporary) |
| R02/R04 physical format and directional measurement | Procedure in `evidence/06-lighting-measurement.md` | Oldest supported and newer physical iPhone | NOT RUN | Required aggregate scalar record; no imagery |

## Human verification remaining

- R02/R04 (OPEN): use the debugger-based aggregate-only procedure on the oldest supported and a newer iPhone. Record device/OS/build, FourCC/range, plane dimensions/stride, clean aperture, viewport, `ios-provisional-v1`, counts, and regional aggregate metrics.
- Expected: actual supported layout, non-inverted directional region differences, nil on insufficient coverage, and no retained/reused metrics. Actual: NOT RUN.
- Calibration, warning usefulness, participant/device coverage, exposure/display/mask confounders, and held-out validation remain OPEN for plan 11. This plan has no visible lighting UI.

## Decisions and research

- R02: OPEN. Synthetic same-frame G02 and one-mirror asymmetric luma tests pass; physical regional direction/clean-aperture evidence is NOT RUN.
- R04: OPEN. Measurement mechanics and canonical range conversion pass synthetic tests; no physical comparison or calibration was performed.
- No D01–D09 decision or `ios-provisional-v1` threshold changed. No R01–R07 gate was closed.

## Next chat

- Plan 07 receives only `FrameObservation.lighting: FaceLightingMetrics?` with canonical 0–255 means, 0–1 fractions, and validated counts. It must never inspect buffers, sample arrays, histograms, or analyzer workspace.
- Nil means unknown for the current frame and must reset according to plan 07’s pure temporal rules; never reuse the previous valid metric.
- Plan 07 owns L04–L10 classification, hysteresis, persistence, badge/banner projections, and lighting-history integration. It must not tune provisional constants or add UI from plan 08.
- R02/R04 may remain open during independent synthetic classifier work, but live regional-direction and lighting release acceptance cannot pass without their evidence.
