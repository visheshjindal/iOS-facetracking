# Plan 00 — completion report

- Plan file: `plans/00-project-foundation.md`
- Date / commit or working-tree identification: 2026-09-21; uncommitted working tree (planning/specification files were already untracked and were preserved)
- Code status: complete for plan 00
- Automated verification: passed after one corrected UI-test fixture assertion
- Human acceptance: pending

## Changes and contracts

- Reconfigured `facetracking.xcodeproj` without changing its name, app bundle identifier, or automatic signing ownership. Debug and Release use iOS 17.0, Swift 6, complete concurrency checking, `SWIFT_DEFAULT_ACTOR_ISOLATION = nonisolated`, iPhone family 1, iOS/iOS Simulator only, and portrait-only iPhone orientation.
- Added the shared `facetracking` scheme, `facetrackingTests`, `facetrackingUITests`, their app dependencies, and scheme test membership. XCTest is the unit-test vocabulary; XCUITest is used for UI automation.
- Moved the single `@main` entry into `facetracking/App/`; added a localized landing screen, a camera-free capture placeholder, and Exit dismissal. Stable control identifiers are `landing.startCapture` and `capture.exit`.
- Added `facetracking/Resources/Localizable.xcstrings` with the section-9 English baseline plus M0 landing/placeholder copy, and retained the asset catalog under `Resources/`.
- Added exact generated-Info setting `NSCameraUsageDescription = Camera access helps position and follow your face on this device.` No microphone or photo permission key was added.
- Added `facetracking/Tracking/TrackingConfiguration.swift`. `TrackingConfiguration.provisional` is the `ios-provisional-v1` value profile. It contains all named algorithm constants from sections 7–8, including hold/gap/freshness/stall/watchdog/EMA timings; geometry, pose, and anchor thresholds; grid/count/ellipse/region values; canonical/video luma and histogram parameters; exposure/imbalance thresholds; and persistence. `CaptureDisplayConfiguration` separately owns the 450 ms mask fade and 44-point action target.
- Added `docs/implementation/toolchain.md`, `decisions.md` (D01–D09), and `research.md` (R01–R07, all OPEN).
- Created required scoped guidance: `facetracking/AGENTS.md`, `facetrackingTests/AGENTS.md`, `facetrackingUITests/AGENTS.md`, and `docs/implementation/AGENTS.md`. Synchronized-group exception sets exclude target-tree `AGENTS.md` files from app/test membership.

## Verification evidence

All Xcode commands used `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` because the host’s active developer directory is Command Line Tools rather than full Xcode.

| Test or requirement ID | Exact command or manual procedure | Environment / destination | Result | Evidence location |
| --- | --- | --- | --- | --- |
| Toolchain discovery | `xcodebuild -version`; `xcrun swift --version`; `xcodebuild -showsdks`; `xcodebuild -list -project facetracking.xcodeproj`; `xcodebuild -project facetracking.xcodeproj -scheme facetracking -showdestinations`; `xcrun simctl list devices available` (each with the pinned `DEVELOPER_DIR`) | Xcode 27.0 (`27A266a`), Swift 6.4, iOS 27 SDK | Passed; three targets/shared scheme discovered; installed runtimes 18.6/26.0/26.5/27.0 | `docs/implementation/toolchain.md` |
| Project format | `plutil -lint facetracking.xcodeproj/project.pbxproj` and Ruby `Xcodeproj::Project.open` target enumeration | Local checkout | Passed; plist OK and all three targets parsed | This handoff; command output was ephemeral |
| M0 configuration smoke | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project facetracking.xcodeproj -scheme facetracking -destination 'platform=iOS Simulator,id=8DFAAEF4-C763-42DD-9DB6-03776F020008' -derivedDataPath /tmp/facetracking-plan00-unit-derived -resultBundlePath /tmp/facetracking-plan00-unit.xcresult -only-testing:facetrackingTests test` | iPhone 16 simulator, iOS 18.6 (`22G86`), arm64 | Passed: 1 test, 0 failures; profile identity and core timing verified | Temporary `/tmp/facetracking-plan00-unit.xcresult` |
| M0 route smoke, first implementation | Same UI command with `/tmp/facetracking-plan00-ui.xcresult` | Same simulator | Failed: container accessibility identifier propagated to child controls, so the test could not find the intended button ID | Temporary `/tmp/facetracking-plan00-ui.xcresult`; corrected without weakening route assertions |
| M0 route smoke, final | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project facetracking.xcodeproj -scheme facetracking -destination 'platform=iOS Simulator,id=8DFAAEF4-C763-42DD-9DB6-03776F020008' -derivedDataPath /tmp/facetracking-plan00-ui-final-derived -resultBundlePath /tmp/facetracking-plan00-ui-final.xcresult -only-testing:facetrackingUITests test` | Same simulator | Passed: 1 test, 0 failures; launch at landing, enter placeholder, Exit returns to landing | Temporary `/tmp/facetracking-plan00-ui-final.xcresult` |
| Unsigned generic-device build | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project facetracking.xcodeproj -scheme facetracking -destination 'generic/platform=iOS' -derivedDataPath /tmp/facetracking-plan00-final-derived CODE_SIGNING_ALLOWED=NO build` | iOS 27 SDK, deployment target iOS 17, arm64 | Passed after the final UI source correction: `BUILD SUCCEEDED` | Temporary DerivedData path above |
| Generated Info.plist and bundle audit | `plutil` extraction from `/tmp/facetracking-plan00-final-derived/Build/Products/Debug-iphoneos/facetracking.app/Info.plist`; `find` audit of the same app bundle | Final unsigned generic-device product | Passed: exact camera string, `MinimumOSVersion=17.0`, `UIDeviceFamily=[1]`, portrait-only iPhone orientation; no Markdown/test sources in app bundle | Generated product in temporary DerivedData |
| Debug/Release target settings | `xcodebuild -project facetracking.xcodeproj -target <each target> -configuration <Debug-or-Release> -showBuildSettings` filtered to deployment/language/isolation/platform/family/bundle values | All three targets, both configurations | Passed: iOS 17, Swift 6, complete concurrency, nonisolated default, family 1, iOS + simulator consistently | Project file and this handoff |
| Camera/privacy shell audit | `rg -n 'AVCaptureSession|requestAccess|AVFoundation|NSMicrophoneUsageDescription|NSPhotoLibraryUsageDescription' facetracking facetracking.xcodeproj || true` | Source/project | Passed: no matches; no session initialization or permission request | Source tree |

The sandboxed unsigned build attempt failed before source compilation because Xcode’s macro/plugin server could not run under `sandbox-exec`; the same exact build passed with Xcode process access. This was an environment restriction, not recorded as a product failure.

## Human verification remaining

### D02/R07 product/device-owner review — NOT RUN

- Required: product/device owner and release context.
- Setup: open `docs/implementation/toolchain.md`, `decisions.md`, generated app settings, and current signing configuration.
- Actions:
  1. Confirm the supported iPhone model list and whether iOS 17.0 is the minimum.
  2. Confirm English baseline and required translations.
  3. Confirm ownership of bundle identifier `xim.facetracking` and automatic signing.
  4. Name available oldest-supported and newer physical test devices/OS versions.
- Expected: explicit approvals or requested changes recorded under D02/R07, without treating simulator/build evidence as physical-device acceptance.
- Actual: NOT RUN.
- Evidence needed: reviewer name/role, date, approved choices, device inventory, and any follow-up decision changes.

### Shell smoke, human simulator/device — NOT RUN

- Gate: plan-00 human shell smoke.
- Required: human observer with an iPhone simulator or signed physical iPhone build.
- Setup: launch a clean build of scheme `facetracking`; no prior camera permission state is required because M0 never requests access.
- Actions:
  1. Confirm the landing screen appears.
  2. Tap **Start capture**.
  3. Confirm the camera-free placeholder appears and tap **Exit**.
  4. Confirm landing returns; repeat the entry/Exit cycle.
  5. Observe system privacy indicators and permission prompts throughout.
- Expected: landing restores on every Exit, no camera prompt, no camera indicator, and no process termination.
- Actual: NOT RUN by a human. The equivalent automated navigation passed, but is not substituted for human observation.
- Evidence needed: simulator/device model, OS, build/commit, observer/date, observed prompt/indicator state, and pass/fail.

### Physical signed-device tests — NOT RUN

- Required: signed physical iPhone UDID and confirmed signing owner.
- Reproducible command is recorded in `docs/implementation/toolchain.md`.
- Expected for M0: app launches and shell smoke behaves as above; no camera access occurs.
- Evidence needed: device/model/OS/build and signed test result. This does not close R01–R07.

## Decisions and research

- D01–D09 are recorded in `docs/implementation/decisions.md`. D02 is a provisional platform choice pending R07; D09 remains explicitly provisional.
- R01–R07 are all `OPEN` in `docs/implementation/research.md`. No physical discovery, calibration, performance, or release gate was closed.
- The simulator proves the shell route and test plumbing only. It does not prove camera, Vision pose, transforms, lighting, freshness latency, multi-person behavior, performance, or device support.

## Next chat

- Next plan and interfaces now available: plan 01 may use `TrackingConfiguration.provisional`, the compiling Swift 6/nonisolated target, XCTest target, shared scheme, and recorded toolchain command.
- Checks that must pass before dependent work: the plan-00 configuration unit test and unsigned device build; add plan-01 P/S tests without real sleeps.
- Human gates that may remain open during independent implementation: D02/R07 product confirmation and all R01–R07 physical/research evidence, subject to the dependency limits in the plan index.
- Known failures and smallest concrete follow-up: no current automated failure. The active `xcode-select` path is Command Line Tools, so keep using the documented `DEVELOPER_DIR` unless the host setting is intentionally changed. An iOS 17 runtime/device is still needed to validate the provisional minimum.
